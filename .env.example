# ZeroDB Configuration
# =====================
#
# ⚠️ SECURITY WARNING:
# This file is a template. Copy it to .env and fill in your actual values.
# NEVER commit your .env file with real API keys to version control.
# Add .env to your .gitignore file immediately.
#
# For production deployments, use a secret manager:
# - AWS Secrets Manager
# - HashiCorp Vault
# - Google Cloud Secret Manager
# - Azure Key Vault
#
# See /SECURITY.md for complete best practices.

# ZeroDB API Credentials
# ----------------------
# Get your API key from: https://ainative.studio/dashboard
# CRITICAL: Keep this secret! Never expose in client-side code.
ZERODB_API_KEY=your_api_key_here

# Your ZeroDB project ID
ZERODB_PROJECT_ID=your_project_id_here

# API Base URL (change only if using a different environment)
ZERODB_BASE_URL=https://api.ainative.studio/v1/public

# Application Configuration
# -------------------------
# Environment (development, staging, production)
ENVIRONMENT=development

# Server Configuration (for backend server)
# ----------------------------------------
PORT=8000
HOST=0.0.0.0

# Logging
# -------
LOG_LEVEL=INFO

# Security Configuration
# ----------------------
# Enable HTTPS only in production
HTTPS_ONLY=true

# CORS allowed origins (comma-separated)
# For production, specify your actual domains
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# JWT Configuration (if using user authentication)
# -----------------------------------------------
# Generate a strong secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your_jwt_secret_here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=30

# Rate Limiting
# -------------
RATE_LIMIT_PER_MINUTE=60

# Circle API Configuration (Issue #114)
# --------------------------------------
# Get your API key from: https://developers.circle.com/
# Use sandbox keys for development/testing
CIRCLE_API_KEY=your_circle_api_key_here
CIRCLE_BASE_URL=https://api-sandbox.circle.com

# Optional: Multi-Environment Setup
# ---------------------------------
# For teams managing multiple environments, use separate .env files:
# - .env.development
# - .env.staging
# - .env.production
#
# Load the appropriate file based on ENVIRONMENT variable
